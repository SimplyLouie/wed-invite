# Local gallery videos

Copy local videos into this folder to add them to the Full Gallery automatically.
Supported formats are `.mp4`, `.mov`, and `.webm`.

For a custom thumbnail, add an image with the same filename. Example:

- `first-dance.mp4`
- `first-dance.jpg`

Featured YouTube videos are managed separately in `data/gallery.ts`.
